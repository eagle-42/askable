//! askable — reads the service's search traces.
//!
//! Nothing here yet: this commit lays the skeleton and nothing else.

fn main() {
    println!("askable 0.1.0 — skeleton. `askable tail` is next.");
}
