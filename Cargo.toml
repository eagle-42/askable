[package]
name = "askable"
version = "0.3.0"
edition = "2024"
description = "A regression judge for ranked search: replay a frozen corpus, record where the right answers landed"

[dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
toml = "1.1"
