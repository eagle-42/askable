[package]
name = "askable"
version = "0.1.0"
edition = "2024"

[dependencies]
serde_json = "1.0"

# No clap, no chrono: two flags parse in twenty lines, and an hour is already at
# the front of every tracing message. A dependency earns its place when the
# surface grows, not before.
